[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pokemon-battle-simulator"
version = "1.0.1"
description = "A Python package for simulating Pokemon battles."
authors = [
    { name = "Dreamsever", email = "your.email@example.com" }
]
license = { text = "MIT" }
